---
import Layout from '../layouts/layout.astro';
import AchievementsComponent from '../components/Achievements.astro';
---

<Layout>
  <AchievementsComponent />
</Layout>

<script is:inline>
  function initAchievementsPage(retries = 0) {
    if (typeof renderAchievements === 'function' && typeof _appInitialised !== 'undefined' && _appInitialised) {
      document.getElementById('screen-achievements').classList.add('active');
      if (typeof applyTranslations === 'function') applyTranslations();
      renderAchievements();
    } else {
      if (retries < 100) {
        setTimeout(() => initAchievementsPage(retries + 1), 50);
      } else {
        console.error('[Numfly Error] Timeout bij laden van achievements.astro functionaliteit.');
      }
    }
  }
  initAchievementsPage();
</script>